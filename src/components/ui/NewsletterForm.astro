---
// src/components/ui/NewsletterForm.astro
---

<div class="flex flex-col sm:flex-row gap-3 max-w-md mx-auto" id="newsletter-form">
  <input
    type="email"
    placeholder="Enter your email..."
    required
    class="nl-input flex-1 px-5 py-3.5 rounded-full text-bark text-sm border-2 border-white/30 focus:border-white bg-white/90 placeholder:text-gray-400 font-medium shadow-md"
    aria-label="Email address"
    id="email-input"
  />
  <button 
    type="submit"
    class="bg-white text-forest hover:bg-sage/20 font-semibold px-7 py-3.5 rounded-full transition-all duration-300 shadow-md hover:shadow-lg hover:-translate-y-0.5 whitespace-nowrap"
    id="submit-btn"
  >
    Subscribe
  </button>
</div>
<p class="text-white/70 text-xs mt-4 font-medium">Unsubscribe anytime. We respect your privacy.</p>

<script>
  const form = document.getElementById('newsletter-form');
  const btn = document.getElementById('submit-btn');
  
  form?.addEventListener('submit', async (e) => {
    e.preventDefault();
    const input = document.getElementById('email-input') as HTMLInputElement;
    
    if (input?.value) {
      btn.textContent = 'Subscribing...';
      btn.disabled = true;
      
      // Simulate API call
      await new Promise(resolve => setTimeout(resolve, 1000));
      
      btn.textContent = 'Subscribed! 🎉';
      btn.classList.add('bg-white');
      input.value = '';
      
      setTimeout(() => {
        btn.textContent = 'Subscribe Free';
        btn.disabled = false;
        btn.classList.remove('bg-white');
      }, 3000);
    }
  });
</script>