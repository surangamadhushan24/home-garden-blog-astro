---
// src/components/ui/NewsletterForm.astro
---

<div class="flex flex-col sm:flex-row gap-3 max-w-md mx-auto" id="newsletter-form">
  <input
    type="email"
    placeholder="Enter your email..."
    required
    class="nl-input flex-1 px-5 py-3.5 rounded-full text-bark text-sm border-2 border-white/30 focus:border-white bg-white/90 placeholder:text-gray-400 font-medium shadow-md dark:text-gray-100 dark:border-gray-600/30 dark:bg-gray-800/90 dark:placeholder:text-gray-500"
    aria-label="Email address"
    id="email-input"
  />
  <button 
    type="submit"
    class="bg-white text-forest hover:bg-sage/20 font-semibold px-7 py-3.5 rounded-full transition-all duration-300 shadow-md hover:shadow-lg hover:-translate-y-0.5 whitespace-nowrap dark:bg-gray-700 dark:text-leaf dark:hover:bg-gray-600"
    id="submit-btn"
  >
    Subscribe
  </button>
</div>
<p class="text-white/70 text-xs mt-4 font-medium dark:text-gray-300/70">Unsubscribe anytime. We respect your privacy.</p>

<script>
  const form = document.getElementById('newsletter-form');
  const btn = document.getElementById('submit-btn');
  
  form?.addEventListener('submit', async (e) => {
    e.preventDefault();
    const input = document.getElementById('email-input') as HTMLInputElement;
    
    if (input?.value) {
      btn.textContent = 'Subscribing...';
      btn.disabled = true;
      
      // Simulate API call
      await new Promise(resolve => setTimeout(resolve, 1000));
      
      btn.textContent = 'Subscribed! 🎉';
      btn.classList.add('bg-white');
      input.value = '';
      
      setTimeout(() => {
        btn.textContent = 'Subscribe Free';
        btn.disabled = false;
        btn.classList.remove('bg-white');
      }, 3000);
    }
  });
</script>